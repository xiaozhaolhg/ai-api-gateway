server:
  port: "8080"
provider:
  providers:
    ollama:
      enabled: true
      endpoint: "http://host.docker.internal:11434"
      api_key: ""
    opencode_zen:
      enabled: true
      endpoint: "https://opencode.ai/zen"
      api_key: ""